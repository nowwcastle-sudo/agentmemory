node_modules/
dist/
*.tsbuildinfo

.env
.env.*
!.env.example

*.log
.DS_Store
.claude/

plugin/scripts/*.map
plugin/scripts/*.d.mts
data/
!eval/data/
!eval/data/**
data-*/
agentmemory-debug/
.gstack/

# Lock files. Upstream never commits them; this fork runs on one desktop and
# is deployed from its own build, so package-lock.json is tracked to make two
# installs the same build (2026-09-11). The other managers stay ignored.
pnpm-lock.yaml
yarn.lock
integrations/hermes/__pycache__/

# Eval reports (transient; published scorecards live in docs/benchmarks/)
eval/reports/
# LongMemEval download is 278MB; fetched on demand
eval/data/longmemeval/
.swe-cycle/
